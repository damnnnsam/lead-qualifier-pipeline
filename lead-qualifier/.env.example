# Required — set via `fly secrets set` for production
DEEPSEEK_API_KEY=
SERPER_API_KEY=
API_KEY=

# Optional
BRIGHTDATA_PROXY=
LLM_PROVIDER=deepseek
LLM_CONCURRENCY=50
MAX_CONCURRENT_JOBS=16
JOB_TIMEOUT_SECONDS=1200

# Optional — only needed if LLM_PROVIDER=claude
# ANTHROPIC_API_KEY=
